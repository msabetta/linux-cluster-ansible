- name: Restart SSHD
  ansible.builtin.systemd:
    name: sshd
    state: restarted
    daemon_reload: true

- name: Ensure sshusers group is active
  ansible.builtin.group:
    name: sshusers
    state: present
    system: yes